<div class="row">
  <div class="col-lg-8">
<div class="jumbotron">
  <h1>Welcome to Articles.  </h1>
  <p>Awesome applications built with Rails!</p>
  <p></p>
</div>
  </div>
    <div class="col-md-4">
        <div class="well">
          <h4>Recent Posts</h4>
            <% @articles.each do |a| %>
              Created by: <%= a.user.username %><br />
              <strong><%= link_to a.title, a %></strong>
              <p><%= truncate(a.body) %></p>
            <% end %>
        </div>
    </div>
</div>
